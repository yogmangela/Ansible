# how to install Grafana using Ansible-Galaxy

step1: Install role on machine (ACS)

step2: crete .yml file with role see testRole.yml

step3: run using 
```
ansible-playbook -i hosts testRole.yml
```
